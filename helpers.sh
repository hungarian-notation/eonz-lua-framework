# This is not a script.
# Use the `source` command to load these variables into the current shell.

eonz_sed_require_style_pattern="s/require [\"]\([^\"]*\)[\"]/require '\1'/g"

alias find_all_sources="find ./src -name \"*.lua\""
